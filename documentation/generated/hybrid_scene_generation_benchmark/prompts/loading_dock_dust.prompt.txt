You invent one neighboring room for a local interactive-fiction prototype.
Return valid JSON only. Do not write markdown fences. Do not return explanations.
The new room must stay inside the same liminal datacenter / desert fiction.
It must be spatially readable, sparse, and suitable for grayscale raytracing.
Do not describe a whole region. Describe one immediate neighboring room only.
The reverse direction back to the source room should usually remain possible.
Keep the title short.
Keep the summary compact and concrete.
Keep arrival_narration under about 60 words and make it immediately playable.
Set move_cost to 1 for a normal successful traversal, and use 0 only if the traversal has no meaningful effect.
Set score_delta conservatively. Use 0 unless entering this room meaningfully advances progress or reveals something important.
visible_objects must contain 3 to 5 concrete actionable objects or interfaces, not vague scenery only.
Include at least two directly usable things such as a hatch, crate, keypad, badge reader, placard, switch, cabinet, console, lockbox or intercom.
Use blocked_exits to mark closed directions explicitly; directions not listed there will be treated as traversable.
For every new room, decide all four cardinal directions. blocked_exits must list every blocked direction explicitly.
The reverse direction back to the source room should usually stay open, so it should usually be absent from blocked_exits.

Current hard state
turn_number: 1
move_count: 0
score: 0
alert_level: 2
cooling_state: stable
water_state: stable
power_state: stable
unresolved_threats: (none)

Current soft state
rolling_summary: A side loading bay opens toward the desert through a half-raised shutter. Pallets, a cargo crate, and a bent lamp post stage a tense threshold between inside and outside.
atmosphere: loading dock

Recent session history
(none)

Current room brief
location_id: unknown
room_title: Dust Loading Dock
room_summary: A side loading bay opens toward the desert through a half-raised shutter. Pallets, a cargo crate, and a bent lamp post stage a tense threshold between inside and outside.
location_archetype: loading dock
canonical_fixture: unknown
time_of_day: dusk
visibility_level: dusty
desert_state: dusty
interior_density: sparse
alert_level: 2
anchors: half-raised shutter, dock lip, desert threshold
visible_objects: cargo crate, shipping placard, control switch, lamp post
blocked_exits: north
spatial_anomalies: dust slips under the shutter in thin sheets

Traversal request
direction: east

Generated room schema
Return exactly one JSON object with these top-level keys:
{
  "title": string,
  "summary": string,
  "arrival_narration": string,
  "move_cost": integer,
  "score_delta": integer,
  "spatial_state": {
    "location_archetype": string,
    "time_of_day": "day" | "dusk" | "night" | "unknown",
    "visibility_level": "clear" | "dusty" | "low" | "unknown",
    "desert_state": "still" | "windy" | "dusty" | "unknown",
    "interior_density": "sparse" | "dense" | "unknown",
    "alert_level": integer,
    "anchors": [string],
    "visible_objects": [string],
    "blocked_exits": ["north" | "east" | "south" | "west"],
    "spatial_anomalies": [string]
  }
}
